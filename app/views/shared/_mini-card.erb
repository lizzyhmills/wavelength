<div class="mini-card carousel-item active" data-scroll-target="card">

  <img src="<%= post.image_url %>" alt="">
  <div class="description-div">
    <span><%= post.song_name.truncate(11)%></span>
    <p>By <%= post.artist.truncate(20) %></p>
  </div>
</div>
