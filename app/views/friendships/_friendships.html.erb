<div class="d-none" data-friends-target="friends" data-controller="follow">
  <div class="mt-3">
    <div class="d-flex justify-content-around">
      <button class="small-button-white-follow" data-action="click->follow#showFollowers" data-follow-target="followersButton">following</button>
      <button class="small-button-white-follow" data-action="click->follow#showFollowing" data-follow-target="followingButton">followers</button>
      <button class="small-button-white-follow" data-action="click->follow#showPending" data-follow-target="pendingButton">pending</button>
    </div>
  </div>
 <%# MY FOLLOWERS %>
 <%= render partial: "followers" %>
 <%# FOLLOWING %>
 <%= render partial: "following" %>
  <%# PENDING %>
 <%= render partial: "pending" %>

</div>
