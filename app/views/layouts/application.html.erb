<!DOCTYPE html>
<html>
  <head>
    <title>Wavelength - For sharing music you love with the people you love, daily</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%#= favicon_link_tag 'wavelength.ico'%>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-vh-100 <%= 'bg-blur' if params[:controller] == 'posts' && params[:action] == 'new' %>" style="background-color:#0C0C0C">
    <%# <%= render "shared/navbar" %>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <%= render "shared/flashes" %>

    <main class="<%= params[:controller].start_with?("devise") || params[:controller] == "pages" ? "background" : "" %>">
      <%= yield %>
    </main>
    <%# TODO: Add logic to not show this on the homepage / devise pages %>
    <%= render 'shared/footer' %>
  </body>
</html>
